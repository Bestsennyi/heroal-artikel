# heroal Artikel — Техническая дорожная карта

Документ фиксирует уже реализованный инженерный фундамент и системные задачи по архитектуре, производительности и надежности данных.

---

## ✅ Что уже сделано

### 1. Стандартизация базы данных (Google Sheets)
- Унифицированы имена всех листов в нижнем регистре: `artikel`, `farben`, `auskunft`, `benutzer`.
- Введена первичная колонка `id` во всех таблицах.
- Стандартизированы ключевые поля (`artikel_nr`, `bild_haupt`, слоты `bild_1`–`bild_4`).
- Языковые колонки каталога приведены к единому стандарту трех языков (`_de`, `_en`, `_ru`).

### 2. Бэкенд и медиа-хранилище (Google Apps Script)
- Развернут `gas/Code.gs` (v4) с динамическим поиском колонок и листов без учета регистра (защита от сбоев при переименовании).
- Жестко зафиксированы целевые папки Google Диска (`FOLDER_ARTIKEL_ID` и `FOLDER_FARBEN_ID`), исключено случайное сохранение фото в корень Диска.
- Реализованы вспомогательные инструменты таблицы: генератор цветных превью (`dummyimage`) и пакетная синхронизация фото из Диска.

### 3. Двухконтурная архитектура и офлайн
- Настроен Service Worker (`sw.js`) с разделением кэша: оболочка (`heroal-shell-v*`) и медиа-чертежи (`heroal-media-v*`).
- Реализована локальная база IndexedDB (`heroal_warehouse_db`, v4): монолитный кэш каталога, оверлей переводов и очередь неотправленных фото (`photoQueue`).
- Инфраструктура i18n переведена строго на 3 рабочих языка: DE (основной), EN, RU (с автоматической валидацией словаря).
- Закреплены строгие правила архитектуры и разработки в `.cursorrules`.

---

## 🚀 Планы на будущее (Техническое развитие)

### Этап 1. Разгрузка терминалов и оптимизация движка
- **Debounce для полей ввода:** задержка 150–200 мс на события ввода в поиске, чтобы процессор терминала не сканировал всю базу на каждое нажатие клавиши при быстрой печати.
- **Очистка памяти от base64-фото:** убрать хранение тяжелых строк фото из оперативной памяти (`appDB`) — сохранять их только в IndexedDB (`photoQueue`) до момента отправки на сервер.
- **Индексация поиска в RAM:** перейти от полного линейного перебора всех строк `O(n)` через `includes()` к простому поисковому индексу (по номеру артикула, штрихкоду и ключевым словам).

### Этап 2. Надежность фоновой синхронизации и кэша
- **Пошаговая предзагрузка чертежей (Chunked Precache):** скачивание чертежей небольшими пачками с сохранением прогресса, чтобы при обрыве или слабом сигнале Wi-Fi на складе закачка не сбрасывалась, а продолжалась с места остановки.
- **Сквозные автотесты (E2E на Playwright):** автоматическая проверка критических сценариев перед релизами:
  - Авторизация по PIN (`1111`) активным пользователем.
  - Полный запуск и выдача артикулов при физически отключенном интернете.
  - Корректная запись фото в очередь `photoQueue` при офлайне.

### Этап 3. Независимость от инфраструктуры Google
- **Модуль-адаптер данных:** изолировать логику сетевых запросов и парсинга CSV от логики приложения, чтобы интерфейс не зависел напрямую от формата ссылок Google Таблиц.
- **Подготовка под локальный сервер (Self-Hosted):** возможность подключения локального бэкенда (например, PocketBase или легковесный сервер с SQLite) для работы терминалов в полностью закрытом контуре предприятия без доступа в интернет.